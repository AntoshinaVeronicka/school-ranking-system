# 🚀 БЫСТРЫЙ СТАРТ

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Создание базы данных

```bash
# Подключитесь к PostgreSQL
psql -U postgres

# Создайте базу данных
CREATE DATABASE education_db;

# Выйдите из psql
\q

# Выполните SQL-скрипт
psql -U postgres -d education_db -f create_database.sql
```

## Шаг 3: Настройка подключения

Откройте файлы `load_schools_to_db.py` и `test_db_connection.py` и измените параметры подключения:

```python
DB_CONFIG = {
    'host': 'localhost',          # Ваш хост
    'port': 5432,                 # Ваш порт
    'database': 'education_db',   # Название БД
    'user': 'postgres',           # Ваш пользователь
    'password': 'your_password'   # Ваш пароль
}
```

## Шаг 4: Проверка подключения

```bash
python test_db_connection.py
```

Если всё настроено правильно, вы увидите:
```
✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ УСПЕШНО!
```

## Шаг 5: Настройка путей к файлам

В файле `load_schools_to_db.py` укажите пути к вашим Excel-файлам:

```python
EXCEL_FILES = [
    '/путь/к/первому/файлу.xlsx',
    '/путь/к/второму/файлу.xlsx'
]
```

## Шаг 6: Загрузка данных

```bash
python load_schools_to_db.py
```

## Что делать, если возникла ошибка?

### Ошибка подключения к БД
- Проверьте, что PostgreSQL запущен
- Проверьте параметры подключения
- Убедитесь, что база данных создана

### Таблицы не найдены
- Выполните `create_database.sql`

### Ошибка чтения Excel
- Проверьте путь к файлу
- Убедитесь, что файл в формате .xlsx
- Проверьте структуру файла (см. README.md)

## Полезные команды SQL

```sql
-- Посмотреть все школы с регионами и муниципалитетами
SELECT * FROM edu.v_schools_full;

-- Посчитать школы по регионам
SELECT * FROM edu.v_schools_count_by_region;

-- Найти школу по части названия
SELECT * FROM edu.v_schools_full 
WHERE school_name ILIKE '%гимназия%';
```

---

📚 Подробная документация в файле **README.md**
